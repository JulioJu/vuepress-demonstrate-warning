{
  "name": "test",
  "version": "1.0.0",
  "main": "./docs/.vuepress/config.ts",
  "license": "MIT",
  "scripts": {
    "dev": "DEBUG=vuepress* yarn vuepress dev ./docs ",
    "build": "DEBUG=vuepress* vuepress build ./docs",
    "serve": "yarn build && anywhere -h localhost -d ./docs/.vuepress/dist"
  },
  "dependencies": {
    "vuepress": "^2.0.0-beta.8"
  },
  "devDependencies": {
    "anywhere": "^1.5.0"
  }
}
